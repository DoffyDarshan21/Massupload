version: '3.9'
services:
  rebate-web:
    build: .
    container_name: rebate-formatter
    ports:
      - '8000:8000'
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
